<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Staff Appraisal | Nathel Finance</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        --primary: #0c6735;
        --dark-bg: #0c1b12;
        --accent-green: #8bc34a;
        --white: #ffffff;
        --text-muted: rgba(255, 255, 255, 0.7);
        --transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
      }
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
      }
      body {
        font-family: "Inter", sans-serif;
        background-color: var(--dark-bg);
      }

      #appraisal-section {
        background: radial-gradient(
          circle at top right,
          var(--primary),
          var(--dark-bg) 85%
        );
        padding: 120px 0;
        text-align: center;
        min-height: 100vh;
        display: flex;
        align-items: center;
      }
      .container {
        width: 90%;
        max-width: 1200px;
        margin: 0 auto;
      }

      /* Timer Box */
      .deadline-timer {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(139, 195, 74, 0.3);
        display: inline-flex;
        gap: 20px;
        padding: 15px 30px;
        border-radius: 50px;
        margin-bottom: 40px;
        color: var(--white);
      }
      .timer-item {
        font-weight: 700;
        color: var(--accent-green);
      }

      .section-header {
        margin-bottom: 80px;
      }
      .section-subtitle {
        color: var(--accent-green);
        font-weight: 700;
        text-transform: uppercase;
        font-size: 0.9rem;
        letter-spacing: 4px;
        display: block;
        margin-bottom: 20px;
      }
      #appraisal-section h2 {
        color: var(--white);
        font-size: 3.5rem;
        letter-spacing: -2px;
        font-weight: 800;
      }

      .appraisal-grid {
        display: flex;
        justify-content: center;
        margin-bottom: 100px;
      }
      .appraisal-card {
        background: var(--white);
        padding: 70px 50px;
        border-radius: 30px;
        box-shadow: 0 30px 60px rgba(0, 0, 0, 0.4);
        transition: var(--transition);
        text-align: left;
        position: relative;
        overflow: hidden;
        max-width: 550px;
      }
      .appraisal-card::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        width: 6px;
        height: 100%;
        background: var(--primary);
      }

      .appraisal-card h3 {
        font-size: 1.8rem;
        margin-bottom: 20px;
        color: var(--dark-bg);
        font-weight: 700;
      }
      .appraisal-card p {
        color: #4a4a4a;
        font-size: 1.1rem;
        line-height: 1.8;
        margin-bottom: 40px;
      }

      .btn {
        display: block;
        padding: 20px;
        border-radius: 15px;
        font-weight: 700;
        text-decoration: none;
        transition: var(--transition);
        text-align: center;
        font-size: 1rem;
        text-transform: uppercase;
        letter-spacing: 1px;
      }
      .btn-primary {
        background: var(--primary);
        color: var(--white);
        cursor: pointer;
      }
      .btn-primary:hover {
        background: var(--dark-bg);
        transform: translateY(-2px);
      }

      /* State for Closed Portal */
      .btn-disabled {
        background: #ccc !important;
        color: #666 !important;
        cursor: not-allowed !important;
        pointer-events: none;
      }

      .management-footer {
        max-width: 800px;
        margin: 0 auto;
        padding-top: 50px;
        border-top: 1px solid rgba(255, 255, 255, 0.1);
      }
      .management-footer p {
        color: var(--text-muted);
        font-style: italic;
        font-size: 1.2rem;
        line-height: 1.8;
        margin-bottom: 20px;
      }
      .signature {
        color: var(--white);
        font-weight: 700;
        text-transform: uppercase;
        font-size: 0.9rem;
        letter-spacing: 2px;
      }
      .signature span {
        color: var(--accent-green);
      }
    </style>
  </head>
  <body>
    <section id="appraisal-section">
      <div class="container">
        <div class="deadline-timer" id="timerContainer">
          <span id="timerLabel">Submission Status:</span>
          <span class="timer-item" id="countdown">Checking date...</span>
        </div>

        <div class="section-header">
          <span class="section-subtitle">Career Progression</span>
          <h2>Staff Self-Appraisal</h2>
        </div>

        <div class="appraisal-grid">
          <div class="appraisal-card">
            <h3>Performance Review</h3>
            <p>
              Professional growth at Nathel Finance is a continuous journey. Use
              this portal to document your achievements and set goals for the
              next half-year.
              <strong>Note:</strong> This portal is only active in late June and
              December.
            </p>
            <a
              href="YOUR_STAFF_FORM_LINK"
              class="btn btn-primary"
              id="appraisalBtn"
              >Start My Appraisal</a
            >
          </div>
        </div>

        <div class="management-footer">
          <p>
            "Honest reflection is the bridge between where you are and where you
            want to be."
          </p>
          <div class="signature">From the <span>Management Desk</span></div>
        </div>
      </div>
    </section>

    <script>
      function checkAppraisalWindow() {
        const now = new Date();
        const month = now.getMonth(); // 0-11 (June is 5, Dec is 11)
        const date = now.getDate();
        const btn = document.getElementById("appraisalBtn");
        const countdown = document.getElementById("countdown");

        // Windows: June 24-30 (Month 5) OR Dec 14-20 (Month 11)
        const isJuneWindow = month === 5 && date >= 24 && date <= 30;
        const isDecWindow = month === 11 && date >= 14 && date <= 20;

        if (isJuneWindow || isDecWindow) {
          btn.classList.remove("btn-disabled");
          countdown.innerHTML = "Portal is OPEN";
        } else {
          btn.classList.add("btn-disabled");
          btn.innerHTML = "Portal Locked";
          countdown.innerHTML = "Next Window: June 24 or Dec 14";
        }
      }
      checkAppraisalWindow();
    </script>
  </body>
</html>
